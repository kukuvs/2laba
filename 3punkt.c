#include <stdio.h>
#include <math.h>

void main() {
  
    float x1, y1, x2, y2;
    float naosi = (x1||y1||x2||y2 == 0 ) ? 0 : 1;
    if (naosi == 0) {
    printf("какая то переменная на прямой");
    }
}
